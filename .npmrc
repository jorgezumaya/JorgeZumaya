# @angular/fire@20 declares peer deps for Angular 20 but this project runs Angular 21.
# This is safe — AngularFire v20 is functionally compatible with Angular 21.
# Remove this line once @angular/fire@21 (stable) is released.
legacy-peer-deps=true
